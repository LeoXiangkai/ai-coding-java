# ai-coding-java Project Onboarding Template v1.1

用于新项目初始化或存量项目接入。目标是把技术栈和验证边界确认清楚，而不是让 Agent 猜。

## 初始化确认项

```text
项目名称：
项目类型：新项目 / 存量项目 / 维护项目
业务仓是否已有 AGENTS.md：
模板注入策略：业务仓入库 / 本地辅助不入库
默认验证等级：轻量 / 标准 / 严格
```

## 技术栈

```text
请选择或输入：
1. 推荐存量企业 Java 基线
   Java 8 + Spring Boot 2.x + Maven + MyBatis-Plus/MyBatis XML + MySQL + Redis
2. 推荐新项目现代基线
   按企业架构当前标准填写
3. 自定义
   Java：
   Spring Boot：
   构建工具：
   ORM / SQL：
   数据库：
   缓存：
   RPC / MQ：
   前端：
```

## 项目边界

```text
核心模块：
禁止修改模块：
数据隔离条件：租户 / 组织 / 学校 / 年度 / 其他
权限模型：
外部系统：
文件存储：
定时任务：
异步任务：
缓存策略：
```

## 命令

```text
编译命令：
测试命令：
启动命令：
接口验证方式：
数据库连接说明：
测试数据来源：
```

## 规则输出

初始化后建议写入业务项目最近的 `AGENTS.md` 或项目画像：

```text
技术栈：
构建/测试/启动：
数据隔离：
验证等级：
P1 豁免规则：
模板注入策略：
最后确认时间：
确认人：
```

